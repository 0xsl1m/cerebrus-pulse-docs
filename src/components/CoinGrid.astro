---
const coins = [
  'BTC', 'ETH', 'SOL', 'DOGE', 'XRP', 'ADA', 'AVAX', 'LINK', 'DOT', 'MATIC',
  'ARB', 'OP', 'APT', 'SUI', 'SEI', 'TIA', 'INJ', 'FET', 'NEAR', 'ATOM',
  'FIL', 'AAVE', 'UNI', 'MKR', 'LDO', 'PEPE', 'WIF', 'JUP', 'ONDO', 'RENDER',
];

const timeframes = ['15m', '1h', '4h'];
---

<div class="section-divider"></div>
<section class="landing-section landing-section-light" id="coins">
  <div class="mx-auto max-w-5xl">
    <h2 class="mb-4 text-center text-3xl font-bold md:text-4xl">
      <span class="gradient-text-subtle">30+ Hyperliquid Perpetuals</span>
    </h2>
    <p class="mb-6 text-center text-gray-400 text-lg">Real-time data across three timeframes</p>

    <div class="mb-10 flex items-center justify-center gap-3">
      {timeframes.map((tf) => (
        <span class="tf-pill">{tf}</span>
      ))}
    </div>

    <div class="flex flex-wrap justify-center gap-2.5">
      {coins.map((coin, i) => (
        <span class="coin-chip" style={`animation-delay: ${i * 30}ms`}>{coin}</span>
      ))}
      <span class="coin-chip text-gray-500">+ more</span>
    </div>

    <p class="mt-8 text-center text-sm text-gray-500">
      Use <code class="text-[#B388FF]">GET /coins</code> for the live list. It's free.
    </p>
  </div>
</section>
